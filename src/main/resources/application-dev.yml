server:
    port: 8081
    address: localhost

logging:
    level.org.springframework.web: debug

spring:
    application:
        name: loes-dev
    datasource:
        url: jdbc:mysql://localhost:3306/loes_new
        driver-class-name: com.mysql.cj.jdbc.Driver
        username: root
        password: 123456
    data:
        redis:
            host: localhost
            port: 6381

mybatis:
    configuration:
        map-underscore-to-camel-case: true

snowflake:
    machine-bit: 0
    data-center-bit: 0

jwt:
    key: loes
    duration-hours: 24
